export interface IPayload {
    id: string
    username: string,
    currentDate: string
}

export interface UserColab {
    id: string,
    username: string,
    email:string,
    password: string,
    lastSignIn: string,
    createdAt: string
}